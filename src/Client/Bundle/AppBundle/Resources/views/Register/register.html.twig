<h2>Register</h2>
<form action="{{ path('client_app_register') }}" method="post">
    {{ form_widget(form._token) }}
    {{ form_widget(form.username) }}
    {{ form_widget(form.email) }}
    {{ form_widget(form.plainPassword) }}

    <button type="submit" class="btn">Register</button>
</form>